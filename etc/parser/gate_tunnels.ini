[gate_tunnel]
nickname = req|ugroup, word
write_depth_buffer = req, int
num_spline_control_points = req, int
x_range = req, float
y_range = req, float
z_range = req, float
min_radius = req, float
max_radius = req, float
far_radius_factor = req, float
min_speed = req, float
max_speed = req, float
time_to_max_speed = req, float
fade_distance = req, float
near_alpha = req, float
far_alpha = req, float
num_t_steps = req, int
num_s_steps = req, int
min_rotation = req, int
max_rotation = req, int
min_rgb = req, byte, byte, byte
max_rgb = req, byte, byte, byte
[layer]
texture = req, word
color = req, byte, byte, byte
near_alpha_factor = req, float
far_alpha_factor = req, float
radius_factor = req, float
u_offset = req, float
v_offset = req, float
du = req, float
dv = req, float
v_scale = req, float
